# Legacy Scripts

These scripts were used before the Docker-based unified setup.
They ran competitors as native binaries in WSL2 while VelesDB ran as an embedded library.

This approach was replaced by the Docker-based setup (docker-compose.yml)
where ALL engines (including VelesDB) run in containers for fair comparison.

Kept for reference only.
